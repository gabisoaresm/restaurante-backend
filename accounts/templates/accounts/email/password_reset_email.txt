Olá, {{ user.first_name|default:user.username }}!

Recebemos uma solicitação para redefinir a senha da sua conta no Restaurante.

Use o token abaixo para criar uma nova senha:

    {{ reset_password_token.key }}

Para redefinir sua senha, envie uma requisição POST para:

    POST /api/accounts/password_reset/confirm/

Com o seguinte corpo (JSON):
    {
        "token": "{{ reset_password_token.key }}",
        "password": "<sua nova senha>"
    }

Este token é válido por 24 horas. Após esse prazo, será necessário solicitar um novo.

Se você não solicitou a redefinição de senha, ignore este e-mail. Sua senha permanecerá a mesma.

Atenciosamente,
Equipe do Restaurante
